services:
  # 1. Our In-Memory Cache / Rate Limiter
  redis:
    image: redis:7
    container_name: mailstream-redis
    ports:
      - "6379:6379"

  # 2. Apache Kafka (Message Broker) - OFFICIAL IMAGE
  kafka:
    image: apache/kafka:3.7.0
    container_name: mailstream-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  # 3. Local Mail Catcher (For safe testing)
  mailpit:
    image: axllent/mailpit
    container_name: mailstream-mailpit
    ports:
      - "8025:8025"
      - "1025:1025"